.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.vercel
.output
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# This project uses npm — keep package-lock.json as the single source of truth
yarn.lock
pnpm-lock.yaml

# Test artifacts
test-results/
playwright-report/

# Local working notes — never published
CLAUDE.local.md
